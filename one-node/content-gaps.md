# CONTENT GAPS — one node

> GENERATED by `build/derive.mjs`. Regenerated on every run, so do not
> add notes here — they would be overwritten. This file reports what
> `content-source.md` is missing. Fixing any of it means editing
> `content-source.md`, which only Poppy does.

Source: `content-source.md`, sha256 `ed8db5069638ef70…`
Checked 112 content blocks, 348 directed `connects to` edges.
The 2 deposit blocks are not checked here: a deposit joins the graph
through `attached to`, and an empty deposit is a slot, not a gap.

---

## Unreachable — 18 ids with no edges in either direction

These have no `connects to` field of their own **and** are never named in
any other part's `connects to`. Isolate can never arrive at them from
anywhere, and they can never lead anywhere. They are present in the data
and renderable on their own; they are simply not in the graph.

- **`variable`** — §1.1, “Variable — four arrows”
  - missing: no `connects to` field; never referenced by another part
- **`elements.birth`** — §2.1, “Born”
  - missing: no `connects to` field; never referenced by another part
- **`software.slot`** — §3, “SOFTWARE — the defenses (slot)”
  - missing: no `connects to` field; never referenced by another part
- **`interface.error-signal`** — §4, “Claude's push on the metaphor”
  - missing: no `connects to` field; never referenced by another part
- **`conflict.her-position`** — §6, “Her position on conflicts generally”
  - missing: no `connects to` field; never referenced by another part
- **`gap.degree`** — §7, “Only Elements has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Elements has”, not its own
- **`gap.environment`** — §7, “Only Elements has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Elements has”, not its own
- **`gap.remedy`** — §7, “Only Elements has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Elements has”, not its own
- **`gap.favorability`** — §7, “Only Elements has”
  - missing: no `connects to` field; never referenced by another part
- **`gap.conditioning`** — §7, “Only Hardware has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Hardware has”, not its own
- **`gap.real-time-signal`** — §7, “Only Hardware has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Hardware has”, not its own
- **`gap.transmission`** — §7, “Only Hardware has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Hardware has”, not its own
- **`gap.split`** — §7, “Only Hardware has”
  - missing: no `connects to` field; never referenced by another part; `source:` inherited from the group “Only Hardware has”, not its own
- **`gap.learning-style`** — §7, “Only Hardware has”
  - missing: no `connects to` field; never referenced by another part
- **`gap.layering-not-comparison`** — §7, “The relationship between the systems — her reframe”
  - missing: no `connects to` field; never referenced by another part
- **`mechanism.capacity-vs-vehicle`** — §8, “Capacity vs vehicle”
  - missing: no `connects to` field; never referenced by another part
- **`mechanism.agency-vocabulary`** — §8, “Uninstall, not heal — her vocabulary for self-work”
  - missing: no `connects to` field; never referenced by another part
- **`design.evaluation-system`** — §9, “The evaluation system — how to judge a reading”
  - missing: no `connects to` field; never referenced by another part

---

## One-way — 5 ids reachable but leading nowhere

Not part of the 18 above. These are named by other parts, so isolate can
reach them, but they carry no `connects to` of their own, so a walk stops
there. Included because it is the same class of gap.

- **`whole`** — “THE WHOLE — as it appears”; referenced by 2 part(s), no `connects to` of its own
- **`signature`** — “Signature — satisfaction”; referenced by 1 part(s), no `connects to` of its own
- **`interface.shape`** — “The work's shape”; referenced by 2 part(s), no `connects to` of its own
- **`gap.time`** — “Only Elements has”; referenced by 2 part(s), no `connects to` of its own
- **`gap.fixed-vs-porous`** — “Only Hardware has”; referenced by 1 part(s), no `connects to` of its own

---

## Notes from the last generator run

**Inferences the generator made, each one logged**

- appendix cut at byte 116055; 4408 bytes excluded from the data
- 11 field line(s) outside any block, ignored: (lead) One authoritative version per thing. | (lead) Plain language only in anything displayed. | (lead) Every part carries a source tag | (lead) The Deposits section is append-only. | (lead) The Technical Reference appendix is for verification only. | (lead) Other people's charts are not in this file. | (lead) Layer → section. | (lead) Each part is a block with an `id`. | (lead) Isolate | (lead) Regions | The explode dial: wired regions stay connected as they spread; sealed capaciti
- sealed.completion: completing region read from the phrase "the pressure center" -> region.pressure
- sealed.material-control: completing region read from the phrase "the outlet" -> region.throat
- sealed.melancholy-swing: completing region read from the phrase "the pressure center" -> region.pressure
- sealed.principles: completing region read from the phrase "the pressure center" -> region.pressure
- sealed.social-openness: completing region read from the phrase "the outlet" -> region.throat
- gap.time: source inherited from the group "Only Elements has" (recorded once, at gap.favorability)
- gap.degree: source inherited from the group "Only Elements has" (recorded once, at gap.favorability)
- gap.environment: source inherited from the group "Only Elements has" (recorded once, at gap.favorability)
- gap.remedy: source inherited from the group "Only Elements has" (recorded once, at gap.favorability)
- gap.fixed-vs-porous: source inherited from the group "Only Hardware has" (recorded once, at gap.learning-style)
- gap.conditioning: source inherited from the group "Only Hardware has" (recorded once, at gap.learning-style)
- gap.real-time-signal: source inherited from the group "Only Hardware has" (recorded once, at gap.learning-style)
- gap.transmission: source inherited from the group "Only Hardware has" (recorded once, at gap.learning-style)
- gap.split: source inherited from the group "Only Hardware has" (recorded once, at gap.learning-style)
